# 📋 Notion 数据库配置指南

本指南将帮助您一步步配置 Notion 数据库，以便与三年级语文学习系统集成。

## 📚 第一步：创建 Notion Integration

### 1. 访问 Notion Integrations 页面

打开浏览器，访问：https://www.notion.so/my-integrations

### 2. 创建新的 Integration

1. 点击页面右上角的 **"+ New integration"** 按钮
2. 填写以下信息：
   - **Name**: `三年级语文学习系统` (或任意名称)
   - **Logo**: 可选
   - **Associated workspace**: 选择你的工作空间
3. 点击 **"Submit"** 创建

### 3. 获取 API Key

创建成功后，你会看到 **"Internal Integration Token"**，格式类似：

```
secret_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

**⚠️ 重要**: 复制并保存这个 Token，这就是你的 `NOTION_API_KEY`

## 🗃️ 第二步：创建生词数据库

### 1. 在 Notion 中创建新数据库

1. 打开 Notion
2. 点击 **"+ New page"**
3. 选择 **"Table"** 创建表格数据库
4. 命名为：`三年级生词库`

### 2. 配置数据库属性

将默认的 "Name" 列改为以下结构：

| 属性名称 | 属性类型 | 是否必需 | 说明 |
|---------|---------|---------|------|
| **生词** | Title | ✅ 必需 | 或使用 "Word"、"词语" |
| **拼音** | Text | ⭐ 推荐 | 或使用 "Pinyin" |
| **释义** | Text | ⭐ 推荐 | 或使用 "Meaning"、"意思" |
| **例句** | Text | ⭐ 推荐 | 或使用 "Example"、"示例" |

#### 如何添加属性：

1. 点击表格右上角的 **"+ "** 按钮
2. 选择属性类型（Title / Text）
3. 输入属性名称
4. 按 Enter 确认

### 3. 添加示例数据

添加一些示例生词数据：

| 生词 | 拼音 | 释义 | 例句 |
|-----|------|------|------|
| 欣赏 | xīn shǎng | 享受美好的事物，领略其中的趣味 | 我们一起欣赏美丽的风景。 |
| 珍贵 | zhēn guì | 价值高；意义大；宝贵 | 时间是最珍贵的财富。 |
| 观察 | guān chá | 仔细察看客观事物或现象 | 我们要学会观察生活中的细节。 |
| 仔细 | zǐ xì | 细心；周密 | 做作业要仔细，不能马虎。 |
| 继续 | jì xù | 连续下去；不中断 | 我们继续学习新的生词。 |

## 🔗 第三步：连接 Integration 到数据库

### 1. 分享数据库

1. 打开刚创建的 `三年级生词库` 数据库
2. 点击页面右上角的 **"Share"** 按钮
3. 在弹出的对话框中，点击 **"Invite"**
4. 找到你创建的 Integration（例如：`三年级语文学习系统`）
5. 点击该 Integration 将其添加

### 2. 获取 Database ID

获取数据库 URL 中的 Database ID：

1. 在数据库页面，点击右上角的 **"⋯"** (更多选项)
2. 选择 **"Copy link"** 复制数据库链接

链接格式类似：
```
https://www.notion.so/workspace/xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx?v=yyyyyy
```

其中 `xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx` 就是你的 **Database ID**（32位字符，不包含连字符）

**提示**: Database ID 在 `notion.so/` 之后，`?v=` 之前的部分

## 📝 第四步：创建学习记录数据库（可选）

如果你想将学习记录保存到单独的数据库：

### 1. 创建新数据库

按照第二步的方法，创建名为 `学习记录` 的新数据库

### 2. 配置属性

| 属性名称 | 属性类型 | 说明 |
|---------|---------|------|
| **生词** | Title | 学习的生词 |
| **拼音** | Text | 拼音 |
| **释义** | Text | 释义 |
| **掌握情况** | Select | 选项：认识、不认识 |
| **学习时间** | Date | 学习时间戳 |

#### 配置 "掌握情况" Select 属性：

1. 添加 Select 类型属性
2. 添加两个选项：
   - `认识`（可以设置为绿色）
   - `不认识`（可以设置为红色）

### 3. 分享并获取 Database ID

重复第三步的操作，将此数据库也分享给 Integration，并获取其 Database ID

## ⚙️ 第五步：配置环境变量

将获取的信息填入 `.env` 文件：

```env
# 从第一步获取的 API Key
NOTION_API_KEY=secret_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 生词数据库的 Database ID
NOTION_DATABASE_ID=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 学习记录数据库的 Database ID（可选）
NOTION_RECORDS_DATABASE_ID=yyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyy

# 服务器端口
PORT=3000
```

**注意**：
- 如果不设置 `NOTION_RECORDS_DATABASE_ID`，学习记录会保存到生词数据库中
- 确保没有多余的空格
- 不要使用引号包裹值

## ✅ 测试配置

启动应用并测试：

```bash
npm start
```

访问 http://localhost:3000 并点击"开始学习"

### 成功的标志：

- ✅ 能够看到从 Notion 加载的生词
- ✅ 点击"认识"或"不认识"后，记录出现在右侧
- ✅ 在 Notion 数据库中能看到新增的记录

### 如果出现问题：

1. **"连接服务器失败"**：
   - 检查服务器是否正在运行
   - 查看控制台的错误信息

2. **"获取生词失败"**：
   - 确认 Integration 已分享到数据库
   - 检查 Database ID 是否正确
   - 确认数据库中有数据

3. **记录保存失败**：
   - 检查数据库属性名称和类型
   - 确认"掌握情况" Select 属性有"认识"和"不认识"选项

## 📱 Notion 模板

为了方便，你可以复制这个模板：

### 生词数据库模板

```
┌─────────┬──────────────┬────────────────────────────────────────────┬─────────────────────────────┐
│ 生词    │ 拼音         │ 释义                                       │ 例句                        │
├─────────┼──────────────┼────────────────────────────────────────────┼─────────────────────────────┤
│ 欣赏    │ xīn shǎng    │ 享受美好的事物，领略其中的趣味             │ 我们一起欣赏美丽的风景。    │
│ 珍贵    │ zhēn guì     │ 价值高；意义大；宝贵                       │ 时间是最珍贵的财富。        │
│ 观察    │ guān chá     │ 仔细察看客观事物或现象                     │ 我们要学会观察生活中的细节。│
│ 仔细    │ zǐ xì        │ 细心；周密                                 │ 做作业要仔细，不能马虎。    │
│ 继续    │ jì xù        │ 连续下去；不中断                           │ 我们继续学习新的生词。      │
└─────────┴──────────────┴────────────────────────────────────────────┴─────────────────────────────┘
```

## 🎓 提示和最佳实践

1. **批量导入数据**: 可以从 Excel 或 CSV 文件导入大量生词
2. **使用视图**: 创建不同的视图来筛选和组织生词
3. **添加标签**: 可以添加"单元"或"课程"属性来分类生词
4. **定期备份**: Notion 支持导出数据，建议定期备份

## 🆘 需要帮助？

如果配置过程中遇到问题：

1. 查看服务器控制台的错误日志
2. 检查 Notion Integration 的权限设置
3. 确认数据库属性配置正确
4. 查阅主 README.md 的常见问题部分

---

配置完成后，您就可以开始使用系统学习三年级语文生词了！🎉
